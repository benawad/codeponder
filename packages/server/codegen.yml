schema: http://localhost:4000/graphql
overwrite: true
generates:
  ./src/types.ts:
    config:
      contextType: ./context#MyContext
    plugins:
      - typescript-common
      - typescript-resolvers
      - typescript-server
